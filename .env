# Default public env vars — committed to repo so $env/static/public resolves
# at build time without any local setup. Override in .env.local (gitignored).

PUBLIC_BET_LOOKUP_ENABLED=false
PUBLIC_BET_LOOKUP_URL=
